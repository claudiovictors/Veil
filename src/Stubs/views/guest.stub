<!DOCTYPE html>
<html lang="{{ config('app.locale') }}">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    {{-- Favicon --}}
    <link rel="shortcut icon" href="{{ asset('logo.svg') }}" type="image/x-icon">

    {{-- Page title — child views override this via @section('title') --}}
    <title>@yield('title', 'Slenix - Veil')</title>

    {{-- Fonts: Poppins (headings) + Roboto (body) --}}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Roboto:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">

    {{-- Icons --}}
    <link href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css" rel="stylesheet">

    {{-- Auth stylesheet --}}
    <link rel="stylesheet" href="{{ asset('css/auth.css') }}">

    {{-- Favicon --}}
    <link rel="shortcut icon" href="{{ asset('logo.png') }}" type="image/x-icon">
</head>

<body class="login-body">

    {{-- ─────────────────────────────────────────────────────────────────
        AUTH CONTAINER
        Centered single-column layout shared by all guest pages
        (login, register, password reset, etc.).
    ───────────────────────────────────────────────────────────────────── --}}
    <main class="auth-container">

        {{-- ── BRAND MARK ──────────────────────────────────────────────── --}}
        <div class="auth-logo-area">
            <img src="{{ asset('logo.png') }}" alt="Slenix Veil" width="40" height="40">
            <span>Slenix</span>
        </div>

        {{-- ── AUTH CARD ────────────────────────────────────────────────── --}}
        {{-- Child views inject their form and heading content here. --}}
        <div class="card">
            @yield('content')
        </div>

    </main>

    <script>
        /**
         * Toggles the visibility of a password input field.
         *
         * Swaps the input type between "password" and "text", and updates
         * the Boxicons eye icon to reflect the current state.
         *
         * @param {string} inputId - The `id` of the password <input>.
         * @param {string} iconId  - The `id` of the Boxicons <i> toggle element.
         * @returns {void}
         */
        function togglePasswordVisibility(inputId, iconId) {
            const input = document.getElementById(inputId);
            const icon  = document.getElementById(iconId);

            if (!input || !icon) return;

            const isHidden = input.type === 'password';

            input.type = isHidden ? 'text' : 'password';
            icon.classList.replace(
                isHidden ? 'bx-show' : 'bx-hide',
                isHidden ? 'bx-hide' : 'bx-show'
            );
        }
    </script>

</body>
</html>